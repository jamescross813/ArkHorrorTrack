<h1><%= @game.title%></h1>

  
 <h2> Order Number: <%= @game.order %></h2>

    <% @game.characters.each do |char| %>
    
    <li>
        <%= link_to char.character_base.name, game_character_path(@game, char) %><br>
    
    </li>
    <% end %>

    <% @game.scenarios.each do |scen| %>
        
        <li>
        <%= link_to scen.name, game_scenario_path(@game, scen) %><br>
        
        </li>
    <% end %>

<%= link_to "User Homepage", user_path(@user) %><br>

<%= link_to "Edit Game", edit_user_game_path(@user) %><br><br>

<%= link_to "Add Character", new_game_character_path(@game) %><br>

<%= link_to "Add Scenario", new_game_scenario_path(@game) %>